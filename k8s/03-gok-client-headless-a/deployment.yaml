apiVersion: apps/v1
kind: Deployment
metadata:
  name: gok-client-headless-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gok-client-headless-a
  template:
    metadata:
      labels:
        app: gok-client-headless-a
    spec:
      containers:
        - name: gok-client
          image: mjpitz/grpc-on-kubernetes:latest
          imagePullPolicy: IfNotPresent
          args:
            - client
            - --target
            - dns:///gok-server-headless-a:8080
