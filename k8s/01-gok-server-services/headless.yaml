apiVersion: v1
kind: Service
metadata:
  name: gok-server-headless
spec:
  clusterIP: None
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: grpc
  selector:
    app: gok-server
